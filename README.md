# Palindrome Checker

UC1 implemented: application startup displays a welcome message and app details.

Compile:
```bash
```
Prerequisites:
- Install Java JDK (version 11+ recommended) and ensure `javac` and `java` are on your `PATH`.

Compile (from project root):
```powershell
javac src\PalindromeChecker.java -d out\classes
```
Run:
Run (from project root):
```powershell
java -cp out\classes PalindromeChecker
```
```bash
Quick run (Windows):
- Use the included `run_java.bat` which compiles and runs the app.
java -cp src PalindromeChecker
```

Next steps: implement interactive palindrome checks and additional use cases.
